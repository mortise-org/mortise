{{- if or .Values.registry.enabled .Values.buildkit.enabled .Values.observer.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "mortise.depsNamespace" . }}
  labels:
    {{- include "mortise.labels" . | nindent 4 }}
{{- end }}
