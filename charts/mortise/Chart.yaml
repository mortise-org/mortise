apiVersion: v2
name: mortise
description: Self-hosted Railway-style deploy platform for Kubernetes
type: application
version: 0.1.0
appVersion: "0.1.0"

# No subchart dependencies. The operator is a polite k8s citizen that speaks
# annotations (ExternalDNS, cert-manager) and standard Ingress API. Infrastructure
# (ingress controller, cert-manager, registry, BuildKit) is the user's
# responsibility — or use `scripts/install.sh` which installs everything.
