---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: previewenvironments.mortise.mortise.dev
spec:
  group: mortise.mortise.dev
  names:
    kind: PreviewEnvironment
    listKind: PreviewEnvironmentList
    plural: previewenvironments
    singular: previewenvironment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.appRef
      name: App
      type: string
    - jsonPath: .spec.pullRequest.number
      name: PR
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.url
      name: URL
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PreviewEnvironment is the Schema for the previewenvironments
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PreviewEnvironment
            properties:
              appRef:
                description: AppRef is the name of the parent App this previews (same
                  namespace).
                type: string
              bindings:
                description: Bindings inherited from the staging environment.
                items:
                  properties:
                    project:
                      description: |-
                        Project, if set, resolves the ref in the matching env namespace of the
                        named Project (`pj-{project}-{env}`) instead of the binder's own env
                        namespace. Enables cross-project bindings (e.g. binding app in `web`
                        project to a db in `infra` project).
                      type: string
                    ref:
                      description: |-
                        Ref is the name of the bound App. By default the ref is resolved within
                        the binder's own project namespace.
                      type: string
                  required:
                  - ref
                  type: object
                type: array
              domain:
                description: Domain for this preview (resolved from App.spec.preview.domain
                  template).
                type: string
              env:
                description: Env vars for the preview Deployment. Inherited from staging,
                  overridable.
                items:
                  properties:
                    name:
                      type: string
                    value:
                      type: string
                    valueFrom:
                      properties:
                        secretRef:
                          type: string
                      type: object
                  required:
                  - name
                  type: object
                type: array
              pullRequest:
                description: PullRequest identifies the PR that triggered this preview.
                properties:
                  branch:
                    type: string
                  number:
                    type: integer
                  sha:
                    type: string
                required:
                - branch
                - number
                - sha
                type: object
              replicas:
                description: Replicas for the preview Deployment. Inherited from staging,
                  overridable.
                format: int32
                type: integer
              resources:
                description: Resources for the preview Deployment. Inherited from
                  staging, overridable.
                properties:
                  cpu:
                    type: string
                  memory:
                    type: string
                type: object
              sourceEnv:
                description: |-
                  SourceEnv is the project environment name the preview was forked from
                  (typically `staging`). Bindings declared in the preview resolve against
                  this env's namespace, so previews share the backing services of their
                  source env rather than spinning up their own.
                type: string
              ttl:
                description: TTL after which the preview is auto-deleted if the PR
                  is still open.
                type: string
            required:
            - appRef
            - pullRequest
            - sourceEnv
            type: object
          status:
            description: status defines the observed state of PreviewEnvironment
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              expiresAt:
                description: ExpiresAt is when this preview will be auto-deleted.
                format: date-time
                type: string
              image:
                description: Image is the built container image reference.
                type: string
              phase:
                description: Phase is the current lifecycle phase.
                enum:
                - Pending
                - Building
                - Ready
                - Failed
                - Expired
                type: string
              url:
                description: URL is the HTTPS endpoint for the preview.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
