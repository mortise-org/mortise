---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: projectmembers.mortise.mortise.dev
spec:
  group: mortise.mortise.dev
  names:
    kind: ProjectMember
    listKind: ProjectMemberList
    plural: projectmembers
    singular: projectmember
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.email
      name: Email
      type: string
    - jsonPath: .spec.role
      name: Role
      type: string
    - jsonPath: .spec.project
      name: Project
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ProjectMember binds a platform user to a Project with a specific role.
          Lives in the Project's control namespace (pj-{project}).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProjectMemberSpec defines the desired state of a ProjectMember.
            properties:
              email:
                description: Email is the platform user's email address.
                type: string
              project:
                description: Project is the name of the owning Project (denormalized
                  for queries).
                type: string
              role:
                description: Role is the member's project-level role.
                enum:
                - owner
                - developer
                - viewer
                type: string
            required:
            - email
            - project
            - role
            type: object
          status:
            description: ProjectMemberStatus defines the observed state of a ProjectMember.
            properties:
              addedAt:
                description: AddedAt is the timestamp when the member was added.
                type: string
              addedBy:
                description: AddedBy is the email of the user who added this member.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
